from setuptools import setup

setup(
   name='qspoc_py',
   version='1.0',
   description='Provides state propagation and control optimization functions',
   author='Yitian Wang',
   author_email='cyitianwang@gmail.com',
   packages=['qspoc_py'],  #same as name
   install_requires=[], #external packages as dependencies
)